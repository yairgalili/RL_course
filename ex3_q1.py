"""
Q*_M' (s, a) = Q*_M (s, a) + f(s)
π*(s) = arg max_a Q*_M(s, a)

arg max_a Q*_M'(s, a) = arg max_a Q*_M(s, a) = π*(s)
Since s is constant w.r.t. a.q
Therefore π* is also an optimal policy for M'.'
"""